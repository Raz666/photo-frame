export type GooglePhotosAlbum = {
  id: string;
  title: string;
};

export type GooglePhotosMediaItem = {
  id: string;
  filename: string;
  mimeType: string;
  baseUrl: string;
};

// TODO: Implement Google Photos API client helpers.
